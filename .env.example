# Backend API URL (required for OAuth and API calls).
# When frontend runs on 8081 and backend on 8080:
VITE_API_BASE_URL=http://localhost:8080

# LiveKit Server URL (For production/cloud, use wss://...)
VITE_LIVEKIT_URL=ws://127.0.0.1:7880
